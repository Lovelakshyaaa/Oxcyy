name: oxcy
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # CORE UTILS
  http: ^1.2.0
  path_provider: ^2.1.2
  permission_handler: ^11.3.1
  rxdart: ^0.27.7

  # UI & DESIGN
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  cached_network_image: ^3.4.1
  glassmorphism: ^3.0.0            # <--- RESTORED!
  fluentui_system_icons: ^1.1.225  # Useful for modern icons
  
  # STATE
  provider: ^6.1.5+1

  # 🎵 AUDIO ENGINE
  just_audio: ^0.10.5
  audio_service: ^0.18.18
  audio_session: ^0.1.21
  on_audio_query: ^2.9.0
  
  # YOUTUBE (Hemant's Fork)
  youtube_explode_dart:
    git:
      url: https://github.com/HemantKArya/youtube_explode_dart.git
      ref: master

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
